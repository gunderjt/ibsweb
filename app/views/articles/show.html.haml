%p#notice= notice

%h1.text-center
  = @article.title
%p
  = @article.date_of_article.strftime("%B %-d, %Y")
.image
  = image_tag @article.image.url(:large) if @article.image.exists?
%p
  = @article.body

= link_to 'Edit', edit_article_path(@article)
\|
= link_to 'Back', root_path